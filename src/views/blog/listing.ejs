<%- include("../includes/head") %>
<%- include("../includes/navigation") %>

<main class="container py-4">
  <%- include("../includes/breadcrumbs") %>
  <%- include("../includes/flash-messages") %>
  
  <div class="row">
    <div class="col-lg-8">
      <h1 class="h3 mb-4"><%= data.title || pageTitle %></h1>
      
      <% if (data.posts && data.posts.length > 0) { %>
        <div class="row g-3">
          <% data.posts.forEach(function(post) { %>
            <div class="col-md-6">
              <%- include("../includes/components/card-post", { post: post }) %>
            </div>
          <% }) %>
        </div>
        <%- include("../includes/pagination") %>
      <% } else { %>
        <div class="text-center py-5 text-muted">
          <i class="bi bi-journal-text fs-1 d-block mb-2"></i>
          <p>Nema postova za prikaz</p>
        </div>
      <% } %>
    </div>
    
    <div class="col-lg-4">
      <div class="card mb-4">
        <div class="card-body">
          <form action="/blog/pretraga" method="GET" role="search">
            <label for="blogListSearch" class="visually-hidden">Pretraži blog</label>
            <div class="input-group">
              <input type="search" id="blogListSearch" name="q" class="form-control" placeholder="Pretraži blog...">
              <button type="submit" class="btn btn-primary" aria-label="Pretraži"><i class="bi bi-search"></i></button>
            </div>
          </form>
        </div>
      </div>
      
      <% if (data.categories && data.categories.length > 0) { %>
        <div class="card mb-4">
          <div class="card-header"><h2 class="h6 card-title mb-0">Kategorije</h2></div>
          <div class="list-group list-group-flush">
            <% data.categories.forEach(function(cat) { %>
              <a href="/blog/kategorija/<%= cat.slug %>" class="list-group-item list-group-item-action"><%= cat.naziv %></a>
            <% }) %>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</main>

<%- include("../includes/footer") %>